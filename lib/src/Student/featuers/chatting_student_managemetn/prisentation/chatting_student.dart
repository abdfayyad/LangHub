import 'package:flutter/material.dart';

class ChattingStudent extends StatelessWidget {
  const ChattingStudent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
